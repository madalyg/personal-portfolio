/** Set to true when the /recognition bio page should be public again. */
export const BIO_PAGE_ENABLED = false;

/** Set to true when the /real-estate page should be public. */
export const REAL_ESTATE_PAGE_ENABLED = false;
